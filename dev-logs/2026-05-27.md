# 开发日志 — 2026-05-27

## 今日摘要
完成 Phase 12/13/14 三大阶段：智能轮廓+颜色量化优化、像素画质量综合优化、场景库功能。同步修复了4个历史 bug。

---

## 完成事项

### Bug 修复（上午）
1. **outline/useOutline 字段名不匹配** — `storage.js` 修复，轮廓开关值正确存储
2. **canvas-editor 初始双重刷新** — 移除同步 `refreshCharacterList()`，消除闪烁
3. **场景导出图片未加载** — 新增 `preloadAllPlacementImages()`，导出前等待加载
4. **regenerateBtn 空值检查** — `currentFile` 为空时提示用户而非 JS 报错

### Phase 12: 智能轮廓 + 颜色量化优化
- 重写 `applyOutline()`：仅非透明vs透明交界绘制轮廓，向内占→向外扩
- 新增 `cleanIsolatedPixels()` + `connectGaps()` + `smoothCorners()`
- 颜色数量选项 8/12/16，复杂图像上限从24→16
- 核心色保护 `#000000/#FFFFFF/#FF0000/#FFFF00/#FFCC99`
- `boostPaletteContrast()` → `injectProtectedColors()` → `mergeSimilarColors()` → `simplifyLargeBlocks()` → `cleanIsolatedColorPixels()`

### Phase 13: 像素画质量综合优化
- `enhanceSymmetry()` — 垂直对称增强，70/30混合，保护>16px不对称特征
- `detectKeyFeatures()` — 高对比小特征检测+量化加权保护
- `finalizeColorBlocks()` — 替换旧 simplifyLargeBlocks + cleanIsolatedColorPixels，合并色块优化逻辑
- `polishOutline()` — 替换旧 smoothCorners，去孤立+45°转角+连断点
- `enhanceColors()` — HSL饱和度+15%，对比度增强，纯黑纯白保护
- UI新增「对称增强」开关

### Phase 14: 场景库功能
- 重写保存逻辑：单场景覆盖→多场景数组存储（localStorage）
- 新增 `loadSceneById()` / `deleteScene()` / `renameScene()`
- 场景库卡片网格：场景名、模板、角色数、日期、编辑/重命名/删除按钮
- 编辑按钮自动跳转场景编辑页并恢复状态

### 版本备份
- `backups/ver1` → 已删除（被 ver2 替代）
- `backups/ver2` — Phase 12 完成版本
- `backups/ver3` — Phase 13+14 完成版本（当前）

---

## 当前架构

```
新管线: 对称增强 → 分块取色 → 关键特征检测 → 加权色板量化
       → 色块优化 → 外轮廓检测 → 轮廓打磨 → 色彩增强 → 输出

场景库: localStorage 数组存储 → 多场景管理 → 跳转编辑器恢复状态
```

---

## 文件变更清单

| 文件 | 今日改动 |
|------|----------|
| `js/pixelate.js` | Phase 12+13：新增 enhanceSymmetry/detectKeyFeatures/finalizeColorBlocks/polishOutline/enhanceColors + 重写 applyPalette/applyOutline；删除 simplifyLargeBlocks/cleanIsolatedColorPixels/smoothCorners |
| `js/canvas-editor.js` | Phase 14：场景 CRUD + 导出前预加载图片修复 + 去除双重刷新 |
| `js/app.js` | Phase 14：新增 loadScenes() 场景库渲染；Bug 2 修复相关 |
| `js/upload.js` | Bug 4 修复 + 对称参数支持 |
| `js/storage.js` | Bug 1 outline 字段名修复 |
| `index.html` | 颜色选项、对称开关、场景库视图、管线描述更新 |

## 待办
- 人工验证所有功能点
- Phase 10 模板系统是否恢复评估
