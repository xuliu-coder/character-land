# 开发日志 — 2026-05-22

## 今日摘要
基于 design-document.md 的8个开发阶段，拆分为 32 个可独立交付的小任务。项目正式启动。

---

## 任务总览

| 阶段 | 任务数 | 状态 |
|------|--------|------|
| Phase 1: 基础框架 | 4 | ✅ 已完成 |
| Phase 2: 角色上传 | 5 | ✅ 已完成 |
| Phase 3: 像素化算法 | 6 | ✅ 已完成 |
| Phase 4: 数据存储 | 5 | ✅ 已完成 |
| Phase 5: 场景编辑器 | 8 | ✅ 已完成 |
| Phase 6: 导出功能 | 3 | ✅ 已完成（集成于Phase 5） |
| Phase 7: 错误处理 | 3 | ✅ 已完成 |
| Phase 8: 测试优化 | 3 | ✅ 已完成 |
| Phase 9: 生成链路优化 | 6 | ✅ 已完成 |
| Phase 10: 双模式像素画重构 | 5 | ✅ 已完成 |
| Phase 11: 6步流水线重构 | 5 | ✅ 已完成 |
| Phase 12: 智能轮廓 + 颜色量化优化 | 4 | ✅ 已完成 |
| Phase 13: 像素画质量综合优化 | 5 | ✅ 已完成 |
| Phase 14: 场景库功能 | 3 | ✅ 已完成 |
| Phase 15: GA4 数据埋点 | 2 | ✅ 已完成 |

状态标记：⬜ 未开始 | 🔵 进行中 | ✅ 已完成 | ⏸️ 暂缓 | ❌ 取消

---

## Phase 1: 基础框架 — 页面结构搭建

### 1.1 创建项目文件结构
- **描述**：按照设计文档 6.3 节创建目录和空文件
- **产出**：`index.html`、`css/styles.css`、`js/` 下8个JS文件、`assets/scenes/`、`assets/icons/`
- **验证**：所有文件路径存在，`index.html` 可被浏览器打开
- **依赖**：无
- **状态**：✅ 已完成

### 1.2 搭建HTML基础框架和导航栏
- **描述**：编写 `index.html` 骨架，包含顶部导航栏（Logo + 三个Tab按钮）、主内容区占位、页脚
- **产出**：完整的 HTML 骨架，导航栏可见
- **验证**：浏览器打开显示导航栏，三个Tab按钮可见
- **依赖**：1.1
- **状态**：✅ 已完成

### 1.3 实现Tab切换功能
- **描述**：编写 `js/app.js`，实现三个Tab（角色生成/场景编辑/资源管理）的点击切换
- **产出**：点击Tab按钮可切换显示对应页面区域
- **验证**：点击每个Tab，对应内容区显示/隐藏正确，激活态样式切换正确
- **依赖**：1.2
- **状态**：✅ 已完成

### 1.4 集成Tailwind CSS和基础样式
- **描述**：引入Tailwind CSS CDN，配置设计系统的颜色变量（黑白灰配色），编写补充的 `css/styles.css`
- **产出**：页面拥有正确的配色和基础样式，按钮/输入框/卡片等组件风格统一
- **验证**：按钮有hover效果，输入框有focus样式，颜色与设计文档5.1一致
- **依赖**：1.2
- **状态**：✅ 已完成

---

## Phase 2: 角色上传 — 图片上传与表单

### 2.1 实现图片上传区域（点击+拖拽）
- **描述**：编写 `js/upload.js`，支持点击选择文件和拖拽文件到上传区域
- **产出**：点击上传区域弹出文件选择框；拖拽文件到区域也能识别
- **验证**：两种方式都能选中文件，文件名正确显示
- **依赖**：1.3
- **状态**：✅ 已完成

### 2.2 实现文件格式和大小验证
- **描述**：在上传流程中加入验证：仅允许 JPG/PNG，限制 ≤5MB
- **产出**：不合法文件被拒绝，弹出对应错误提示
- **验证**：上传GIF/TIFF → 格式错误提示；上传 >5MB 图片 → 大小超限提示
- **依赖**：2.1
- **状态**：✅ 已完成

### 2.3 实现原图预览功能
- **描述**：文件通过验证后，在预览区显示原图
- **产出**：预览区展示上传的图片（等比缩放、居中显示）
- **验证**：上传图片后预览区立即显示图片，不再显示占位文字
- **依赖**：2.1
- **状态**：✅ 已完成

### 2.4 实现角色信息表单及验证
- **描述**：四个字段（名称必填、出处、描述、台词）的输入和校验
- **产出**：表单可正常输入，名称为空时点击生成给出提示
- **验证**：名称为空点击生成 → 提示"请输入角色名称"；填写完整后无提示
- **依赖**：1.3
- **状态**：✅ 已完成

### 2.5 实现错误弹窗和成功提示组件
- **描述**：编写 `js/ui.js`，封装可复用的错误弹窗和成功弹窗
- **产出**：`showError(title, message)` 和 `showSuccess(message)` 两个函数
- **验证**：调用函数弹出对应弹窗，点击关闭/外部区域可关闭
- **依赖**：1.2
- **状态**：✅ 已完成

---

## Phase 3: 像素化算法 — Canvas核心

### 3.1 实现图片加载和缩放
- **描述**：编写 `js/pixelate.js`，将上传图片加载到离屏Canvas并缩放到目标尺寸
- **产出**：`loadImageToCanvas(imageSource, targetSize)` 函数
- **验证**：传入图片，输出缩放到128×128的Canvas图像数据
- **依赖**：2.1
- **状态**：✅ 已完成

### 3.2 实现分块取色平均算法
- **描述**：按pixelSize对图像分块，每块取RGB平均值
- **产出**：`pixelate(imageData, pixelSize)` 函数
- **验证**：输入原图ImageData，输出像素化后的ImageData，肉眼可见像素块效果
- **依赖**：3.1
- **状态**：✅ 已完成

### 3.3 实现色板映射（可选增强）
- **描述**：将平均色映射到最近的调色板颜色，增强像素复古感
- **产出**：`applyPalette(imageData, colorCount)` 函数
- **验证**：调色板颜色数设为16时，输出图片颜色种类不超过16
- **依赖**：3.2
- **状态**：✅ 已完成

### 3.4 实现轮廓增强（可选增强）
- **描述**：检测色块边缘，添加深色轮廓线
- **产出**：`applyOutline(imageData, pixelSize)` 函数
- **验证**：开启轮廓后，角色边缘清晰可见深色线
- **依赖**：3.2
- **状态**：✅ 已完成

### 3.5 添加可调参数控件
- **描述**：在生成预览区添加参数调整UI（像素块大小滑块、颜色数量选择）
- **产出**：用户可调整像素块大小(4/8/16)和颜色数量(8/16/32/64)
- **验证**：调整参数后实时更新预览（或点击"重新生成"后更新）
- **依赖**：3.2
- **状态**：✅ 已完成

### 3.6 实现"确认保存"和"重新生成"按钮功能
- **描述**：确认保存触发存储；重新生成用随机参数或用户调整后的参数重新像素化
- **产出**：两个按钮功能完整，生成中显示加载状态
- **验证**：点击确认保存 → 角色进入角色库；点击重新生成 → 预览更新为新效果
- **依赖**：3.2、4.2
- **状态**：✅ 已完成

---

## Phase 4: 数据存储 — IndexedDB集成

### 4.1 封装IndexedDB操作
- **描述**：编写 `js/storage.js`，封装数据库打开、增删改查操作
- **产出**：`db.addCharacter(data)`、`db.getAllCharacters()`、`db.updateCharacter(id, data)`、`db.deleteCharacter(id)` 等API
- **验证**：每个API可正常调用，返回Promise
- **依赖**：无
- **状态**：✅ 已完成

### 4.2 实现角色数据保存
- **描述**：用户确认保存时，将角色信息+原图+像素图存入IndexedDB
- **产出**：角色数据成功写入数据库
- **验证**：保存后刷新页面，数据仍在数据库中（通过浏览器DevTools验证）
- **依赖**：4.1、3.6
- **状态**：✅ 已完成

### 4.3 实现角色数据读取和列表展示
- **描述**：从IndexedDB读取所有角色，在角色库视图和场景编辑器侧栏中以卡片形式展示
- **产出**：角色库卡片列表，显示像素图缩略图+名称+出处
- **验证**：已保存的角色出现在角色库中，顺序正确
- **依赖**：4.1
- **状态**：✅ 已完成

### 4.4 实现角色编辑和删除
- **描述**：编辑按钮可修改角色信息；删除按钮二次确认后删除
- **产出**：编辑弹出表单可修改并保存；删除后角色从列表移除
- **验证**：编辑后信息更新；删除后角色不再出现在角色库中
- **依赖**：4.3
- **状态**：✅ 已完成

### 4.5 实现存储降级方案
- **描述**：检测IndexedDB不可用时自动降级到LocalStorage
- **产出**：`storage.js` 自动选择可用存储方案
- **验证**：模拟IndexedDB不可用场景，应用仍能正常存取数据
- **依赖**：4.1
- **状态**：✅ 已完成

---

## Phase 5: 场景编辑器 — Canvas画布与交互

### 5.1 搭建场景编辑页面三栏布局
- **描述**：左侧资源面板（角色库+场景选择）、中间Canvas画布、右侧属性面板
- **产出**：三栏布局完整，比例 2:8:2
- **验证**：页面显示三栏结构，布局与设计文档3.2.1一致
- **依赖**：1.3
- **状态**：✅ 已完成

### 5.2 实现Canvas画布和网格背景
- **描述**：编写 `js/canvas-editor.js`，初始化Canvas，绘制20×20px网格线
- **产出**：Canvas尺寸800×600，网格线可见
- **验证**：画布正确渲染，网格间距为20px
- **依赖**：5.1
- **状态**：✅ 已完成

### 5.3 实现角色从库拖入画布
- **描述**：编写 `js/drag-drop.js`，角色库中的角色卡片可拖拽到Canvas上
- **产出**：从角色库拖拽角色到画布，角色出现在释放位置
- **验证**：拖拽角色到画布 → 角色像素图出现在画布对应位置
- **依赖**：5.2、4.3
- **状态**：✅ 已完成

### 5.4 实现角色选中和移动
- **描述**：点击画布上的角色选中（显示选中框），拖拽移动位置
- **产出**：角色可被选中（蓝色边框），可拖拽移动
- **验证**：点击角色 → 显示选中框 + 属性面板更新；拖拽角色 → 角色位置实时跟随
- **依赖**：5.3
- **状态**：✅ 已完成

### 5.5 实现角色旋转和缩放
- **描述**：通过属性面板滑块或直接在画布上操作调整角色旋转角度和缩放
- **产出**：角色可按指定角度旋转和比例缩放显示
- **验证**：拖动旋转滑块 → 角色实时旋转；拖动缩放滑块 → 角色实时缩放
- **依赖**：5.4
- **状态**：✅ 已完成

### 5.6 实现属性面板数据绑定
- **描述**：选中角色时，右侧面板显示该角色的名称、X、Y、旋转、缩放等信息
- **产出**：选中不同角色时面板数据实时切换；修改面板数值时画布角色同步更新
- **验证**：选中角色A → 面板显示A的数据 → 修改X坐标 → 角色A移动到新位置
- **依赖**：5.4
- **状态**：✅ 已完成

### 5.7 实现场景模板切换
- **描述**：提供4个预设场景模板选择，切换时更新Canvas背景
- **产出**：点击不同场景模板 → Canvas背景切换为对应场景图
- **验证**：选择"草地" → 画布显示草地背景；画布上的角色位置不受影响
- **依赖**：5.2
- **状态**：✅ 已完成

### 5.8 实现撤销功能
- **描述**：支持 Ctrl+Z 撤销最近的操作（移动/删除/旋转等），最多10步
- **产出**：操作历史栈，Ctrl+Z 回退到上一步状态
- **验证**：移动角色 → Ctrl+Z → 角色回到原位；连续Ctrl+Z多次正确回退
- **依赖**：5.4
- **状态**：✅ 已完成

---

## Phase 6: 导出功能 — 场景导出为PNG

### 6.1 实现Canvas内容导出
- **描述**：编写 `js/export.js`，将Canvas画布（含背景+所有角色）渲染并导出
- **产出**：`exportScene(resolution)` 函数，返回包含所有内容的DataURL
- **验证**：导出的图片包含背景模板和所有角色，位置/大小/旋转与画布一致
- **依赖**：5.2
- **状态**：✅ 已完成

### 6.2 实现分辨率选择
- **描述**：导出前弹出选择框，可选1x(800×600)或2x(1600×1200)
- **产出**：分辨率选择UI，选择后按对应分辨率导出
- **验证**：选2x导出 → 图片尺寸为1600×1200
- **依赖**：6.1
- **状态**：✅ 已完成

### 6.3 实现浏览器下载触发
- **描述**：生成Blob后触发浏览器下载，文件名包含时间戳
- **产出**：点击导出 → 浏览器弹出下载，文件名如 `character-land-20260522-143000.png`
- **验证**：下载的PNG文件可正常打开，内容正确
- **依赖**：6.1
- **状态**：✅ 已完成

---

## Phase 7: 错误处理 — 异常场景覆盖

### 7.1 完善所有错误提示
- **描述**：检查所有功能点的错误路径，确保每个错误都有清晰的用户提示
- **产出**：对照设计文档3.5节完成所有错误处理
- **验证**：逐一触发每种错误场景，确认提示信息正确且可关闭
- **依赖**：Phase 2-6 全部完成
- **状态**：✅ 已完成

### 7.2 实现存储空间检测
- **描述**：应用启动时检测IndexedDB可用空间，不足时提醒用户清理
- **产出**：存储使用量超过80%时弹出提醒
- **验证**：模拟存储空间不足 → 收到清理提醒
- **依赖**：4.1
- **状态**：✅ 已完成

### 7.3 实现IndexedDB不可用降级
- **描述**：当IndexedDB完全不可用时，自动切换LocalStorage并提示用户
- **产出**：降级方案无缝切换，用户无感知（可选轻提示）
- **验证**：禁用IndexedDB后应用仍正常运行
- **依赖**：4.5
- **状态**：✅ 已完成

---

## Phase 8: 测试优化 — 兼容性与性能

### 8.1 跨浏览器兼容性测试
- **描述**：在Chrome、Edge、Firefox最新版中测试全部功能
- **产出**：测试报告，记录各浏览器表现和问题
- **验证**：三大浏览器核心功能均正常
- **依赖**：Phase 1-7 全部完成
- **状态**：✅ 已完成

### 8.2 性能优化
- **描述**：优化像素化算法（目标≤2s）、拖拽响应（目标<100ms）、页面加载（目标<3s）
- **产出**：性能指标达标，慢操作有加载状态
- **验证**：用Performance面板测量各操作耗时
- **依赖**：Phase 1-7 全部完成
- **状态**：✅ 已完成

### 8.3 UI细节打磨
- **描述**：过渡动画、hover效果、焦点状态、空状态提示等细节优化
- **产出**：整体体验流畅、视觉统一
- **验证**：人工走查全部页面，确认无视觉瑕疵
- **依赖**：Phase 1-7 全部完成
- **状态**：✅ 已完成

---

## Phase 9: 生成链路优化 — 提升角色完整性与像素风可读性

### 9.1 添加透明棋盘格预览背景
- **描述**：在 `styles.css` 添加 `.checkerboard-bg` 类，应用到 `#preview-container` 和角色库卡片
- **产出**：透明PNG在预览区正确显示棋盘格背景，不再呈现黑底
- **验证**：上传图片→提取主体→预览区显示棋盘格背景+透明角色主体
- **依赖**：无
- **状态**：✅ 已完成

### 9.2 改写裁切逻辑：宽高比自适应
- **描述**：新增 `getSubjectBounds()` 和 `loadImageToFitCanvas()`，替换 `Math.min(w,h)` 正方形裁切为外接框+安全留白+宽高比保持+居中缩放
- **产出**：角色根据主体外接框自适应裁切，保持宽高比放入128×128画布
- **验证**：高瘦/宽扁角色完整显示，不被截断
- **依赖**：9.1
- **状态**：✅ 已完成

### 9.3 像素化+调色板保留透明度
- **描述**：重写 `pixelate()`/`applyPalette()`/`applyOutline()`，可见像素计算RGB均值，全像素平滑alpha，透明区域保持透明
- **产出**：像素化后保留透明通道，主体边缘无黑边
- **验证**：导出PNG背景透明；角色内部有轮廓，外部边缘无轮廓
- **依赖**：9.2
- **状态**：✅ 已完成

### 9.4 实现真正的中位切分算法
- **描述**：重写 `medianCut()` 为真正的加权中位切分，替换旧的频率排序法；新增 `computeBucketRange()`
- **产出**：颜色量化质量显著提升，肤色/渐变色更准确
- **验证**：同参数对比旧版，颜色保真度明显提升
- **依赖**：9.3
- **状态**：✅ 已完成

### 9.5 图像复杂度检测+失败兜底
- **描述**：新增 `analyzeImageComplexity()`，根据可见像素占比和唯一颜色数分级；复杂度过高时reject特定错误；简单图像跳过减色；`upload.js` 处理 `SUBJECT_TOO_SMALL` 错误码
- **产出**：复杂图片给出明确中文提示，永不返回残缺图
- **验证**：上传复杂图片→提示"建议上传单人正面图..."；简单图片正常处理
- **依赖**：9.2、9.3、9.4
- **状态**：✅ 已完成

### 9.6 集成联调+参数调优
- **描述**：定义可调常量（ALPHA_THRESHOLD、COMPLEXITY_*、SIMPLE_SKIP_PALETTE_COLORS），端到端回归测试，确保旧数据兼容
- **产出**：全流程正常，参数可调，旧数据无报错
- **验证**：完整流程+全参数组合+手动裁剪+旧数据兼容
- **依赖**：9.1-9.5
- **状态**：✅ 已完成

---

## Phase 10: 双模式像素画重构 — 模板模式 + 通用模式

### 10.1 新建像素工具模块
- **描述**：新建 `js/pixel-utils.js`，提供严格颜色量化(Median Cut)、孤立像素清理、硬边轮廓检测、离散方向阴影、最近邻缩放等像素画底层操作
- **产出**：`window.App.pixelUtils` 命名空间，8个可复用函数
- **验证**：每个函数可独立测试
- **依赖**：无
- **状态**：✅ 已完成

### 10.2 新建 Q 版模板系统
- **描述**：新建 `js/pixel-templates.js`，定义48×48二头身Q版模板（头部/头发/眼睛/嘴/身体/手臂/腿）+ 颜色提取 + 特征推断 + 模板适用性判断 + 模板渲染 + 通用模式渲染
- **产出**：`window.App.templates` 命名空间，支持模板模式和通用模式两种渲染路径
- **验证**：模板渲染输出48×48硬边像素画，Q版比例，≤12色
- **依赖**：10.1
- **状态**：✅ 已完成

### 10.3 重写通用模式管线
- **描述**：在 Phase 9 基础上强化：TARGET_SIZE 128→48，严格 ≤12 色，硬边轮廓 + 离散阴影 + 杂色清理
- **产出**：通用模式生成48×48像素画，透明背景，硬边纯色块
- **验证**：输出无渐变、无抗锯齿、无孤立像素
- **依赖**：10.1
- **状态**：✅ 已完成

### 10.4 实现模式选择 + 流程编排
- **描述**：重写 `generatePixelArt()` 为双模式架构：复杂度检测 → 模板适用性判断 → 模板模式/通用模式自动选择 → 返回 `{ dataURL, mode, outputSize }`
- **产出**：自动模式选择，模板优先通用兜底，失败拒绝
- **验证**：动漫角色→模板模式，照片→通用模式
- **依赖**：10.2、10.3
- **状态**：✅ 已完成

### 10.5 UI 适配 + 清理
- **描述**：简化参数面板（移除颜色数/轮廓，新增输出尺寸选择器32/48px），预览区模式标签，删除 `js/drag-drop.js` 和 `js/export.js` 空占位文件
- **产出**：UI 匹配新架构，代码清洁
- **验证**：像素预览清晰无模糊，模式标签正确，尺寸切换生效
- **依赖**：10.4
- **状态**：✅ 已完成

---

## Phase 11: 6步流水线重构 — K-Means + 最近邻 + 智能轮廓

### 11.1-11.3 重写 pixel-utils.js
- **描述**：完整重写 `js/pixel-utils.js`：新增 `preprocessImage()`(512标准化+3×3模糊)、`quantizeColors()`(K-Means++聚类)、`downsampleNearest()`(严格最近邻)、`generateSmartOutline()`(4-neighbor色差法)、`postProcessPixelArt()`(3轮清理+色块规整+边缘闭合)；移除旧的 Median Cut 量化函数
- **产出**：6步流水线所需全部函数
- **验证**：每个函数可独立调试
- **依赖**：无
- **状态**：✅ 已完成

### 11.4 重写主流程 + 删除模板系统
- **描述**：重写 `pixelate.js:generatePixelArt()` 为6步流程（预处理→量化→下采样→轮廓→后处理→输出）；删除 `js/pixel-templates.js` 及 HTML 引用
- **产出**：统一流水线，移除双模式架构
- **验证**：管线串联正确，无对已删除模块的引用
- **依赖**：11.1-11.3
- **状态**：✅ 已完成

### 11.5 UI 适配
- **描述**：`upload.js` 参数增加 `colorCount`，简化 `displayPixelResult`；`index.html` 参数面板新增颜色数量选择器(8/12/16/24)和更多输出尺寸(32/48/64/128)
- **产出**：用户可控的颜色数和输出尺寸
- **验证**：参数切换生效，预览清晰
- **依赖**：11.4
- **状态**：✅ 已完成

---

## 依赖关系图

```
Phase 1 (基础框架)
  ├── 1.1 → 1.2 → 1.3 → 2.1, 2.4, 5.1
  │              └── 1.4
  └── 1.2 → 2.5

Phase 2 (角色上传)
  ├── 2.1 → 2.2, 2.3, 3.1
  ├── 2.4 (独立)
  └── 2.5 (独立)

Phase 3 (像素化算法)
  └── 3.1 → 3.2 → 3.3, 3.4 → 3.5 → 3.6 (需4.2)

Phase 4 (数据存储)
  └── 4.1 → 4.2, 4.3, 4.5
       └── 4.3 → 4.4
       └── 4.5 → 7.3

Phase 5 (场景编辑器)
  └── 5.1 → 5.2 → 5.3 (需4.3) → 5.4 → 5.5, 5.6, 5.8
       └── 5.2 → 5.7

Phase 6 (导出)
  └── 6.1 (需5.2) → 6.2, 6.3

Phase 7 (错误处理) — 需Phase 2-6
Phase 8 (测试优化) — 需Phase 1-7
```

---

## 执行建议

1. **严格按Phase顺序执行**，每个Phase完成后人工验证再进入下一Phase
2. **每个任务独立完成并验证**后再开始下一个
3. **每完成一个任务**，将状态从 ⬜ 更新为 ✅
4. **遇到阻塞**，标记为 ⏸️ 并记录原因
5. **每天结束**，在本目录创建新的日志文件 `YYYY-MM-DD.md`，记录当日进展

---

## Phase 12: 智能轮廓 + 颜色量化优化

### 12.1 重写外轮廓检测 — 外轮廓 + 去毛刺 + 连断点
- **描述**: 重写 `applyOutline()`，只保留非透明vs透明交界轮廓，新增 `cleanIsolatedPixels()` 和 `connectGaps()`
- **产出**: 外轮廓线连续、无内部杂线、无孤立像素
- **验证**: 上传角色 → 轮廓线仅在角色外边缘
- **状态**: ✅ 已完成

### 12.2 轮廓绘制位置向外扩展 1px
- **描述**: 轮廓从非透明像素上改为绘制在邻接的透明像素上
- **产出**: 轮廓向外扩展，角色本体不被压缩
- **验证**: 角色边缘清晰，不显瘦
- **状态**: ✅ 已完成

### 12.3 角平滑处理
- **描述**: 新增 `smoothCorners()`，删除突出直角 + 填充凹陷缺口 + 保留45°斜线
- **产出**: 轮廓转角和曲线平滑
- **验证**: 轮廓无锯齿直角台阶
- **状态**: ✅ 已完成

### 12.4 颜色量化优化
- **描述**: 12-16色、核心色保护、相似色合并、大色块简化、杂色清理、强制不透明
- **产出**: `boostPaletteContrast()`, `injectProtectedColors()`, `mergeSimilarColors()`, `simplifyLargeBlocks()`, `cleanIsolatedColorPixels()`
- **验证**: 颜色纯净，无过渡色噪点
- **状态**: ✅ 已完成

---

## Phase 13: 像素画质量综合优化

### 13.1 对称增强
- **描述**: 新增 `enhanceSymmetry()`，自动检测垂直对称轴，左右70/30混合，保护>16px不对称特征
- **产出**: 对称开关，默认开启
- **验证**: 正面角色左右对称，单侧举手保留
- **状态**: ✅ 已完成

### 13.2 关键特征保护
- **描述**: 新增 `detectKeyFeatures()`，亮度差>50且面积<16px标记为关键特征，量化时强制保留
- **产出**: 眼睛、纽扣等小特征不丢失
- **验证**: 小面积高对比区域清晰可见
- **状态**: ✅ 已完成

### 13.3 色块最终优化
- **描述**: 新增 `finalizeColorBlocks()`，替换旧函数，删孤立像素 + 小色块合并 + 大色块2色简化 + 断裂填充
- **产出**: 色块连续完整
- **验证**: 无杂点，大色块≤2色
- **状态**: ✅ 已完成

### 13.4 轮廓最终打磨
- **描述**: 新增 `polishOutline()`，替换 `smoothCorners()`，删除1邻居孤立像素 + 90°转45° + 连断点
- **产出**: 轮廓连续平滑
- **验证**: 轮廓无断点、无毛刺
- **状态**: ✅ 已完成

### 13.5 色彩增强
- **描述**: 新增 `enhanceColors()`，RGB→HSL，饱和度+15%，对比度增强，纯黑纯白保护
- **产出**: 画面饱和度提升
- **验证**: 色彩鲜艳，黑白纯净
- **状态**: ✅ 已完成

---

## Phase 14: 场景库功能

### 14.1 场景 CRUD
- **描述**: 重写 `save-scene-btn` 为多场景保存，新增 `loadSceneById`/`deleteScene`/`renameScene`，localStorage 数组存储
- **产出**: 场景保存/加载/删除/重命名 API
- **验证**: 保存场景→关闭浏览器→重新打开→加载场景→画布恢复
- **状态**: ✅ 已完成

### 14.2 场景库 UI
- **描述**: 重写 `#resource-scenes` 为卡片网格，显示场景名/模板/角色数/日期，编辑/重命名/删除按钮
- **产出**: 场景库可视化管理
- **验证**: 场景库列表可见，操作按钮正常
- **状态**: ✅ 已完成

### 14.3 场景跳转编辑
- **描述**: 点击编辑按钮 → 加载场景数据 → 自动切换到场景编辑 Tab → 恢复画布状态
- **产出**: 场景库到编辑器的无缝跳转
- **验证**: 点击编辑 → 画布显示保存的角色布局
- **状态**: ✅ 已完成

---

## Phase 15: GA4 数据埋点 — 用户行为追踪

### 15.1 创建埋点模块 + HTML 集成
- **描述**: 新建 `js/analytics.js`，封装 `track()`/`trackPageView()` 安全上报函数；`index.html` `<head>` 引入 GA4 script，底部引入 analytics.js
- **产出**: `window.App.analytics` API，GA4 自动 PV 追踪
- **验证**: F12 Console 输入 `dataLayer` 能看到 page_view 事件
- **依赖**: 无
- **状态**: ✅ 已完成

### 15.2 埋点覆盖核心功能流程
- **描述**: 在 upload.js / canvas-editor.js / app.js 关键操作点添加 13 个埋点事件
- **产出**: character_generate_start/done/fail、character_save/export/edit/delete、scene_create/save/export、tab_switch、storage_mode
- **验证**: GA4 后台实时报告能看到事件数据
- **依赖**: 15.1
- **状态**: ✅ 已完成

---
