# 产品设计文档：我的角色世界 (Character Land)

---

## 1. 产品概述

### 1.1 产品定位
"我的角色世界"是一个**纯前端Web应用**，让用户可以上传喜欢的影视/ACG角色图片，自动生成像素风Q版小人，并将来自不同作品的角色自由摆放在同一个场景空间中。核心体验是"跨作品角色共居"的治愈感和收藏乐趣。

### 1.2 目标用户
- 16-30岁ACG、影视、游戏爱好者
- 喜欢收藏角色形象、有展示欲望
- 对像素风格有审美偏好

### 1.3 MVP范围
| 包含 | 不包含（后续版本） |
|------|-------------------|
| 角色上传 + 简单像素化生成 | AI驱动的Q版重绘 |
| 场景编辑器（拖拽/旋转/缩放） | 用户注册登录 |
| 场景导出为PNG图片 | 云端同步/跨设备访问 |
| 本地存储（IndexedDB） | 社交分享功能 |
| 预设场景模板 | 多场景管理 |
| 角色库管理（编辑/删除） | 用户上传自定义背景 |

---

## 2. 用户流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  上传角色图片  │ ──▶ │  填写角色信息  │ ──▶ │  生成像素小人  │
│  (JPG/PNG)   │     │ 名称/出处/描述 │     │  (Canvas算法)  │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                               │
                                               ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  导出PNG图片  │ ◀── │  拖拽摆放角色  │ ◀── │  保存到角色库  │
│             │     │  选择场景模板  │     │             │
└─────────────┘     └─────────────┘     └─────────────┘
```

**详细步骤：**
1. 用户在"角色生成"页面上传角色图片（支持JPG/PNG，≤5MB）
2. 填写角色信息：名称（必填）、出处、描述、经典台词
3. 点击"生成像素形象"，系统用Canvas算法将图片像素化
4. 预览像素效果，满意后确认保存到角色库
5. 切换到"场景编辑"页，从角色库拖拽小人到Canvas画布
6. 选择预设场景模板作为背景
7. 调整角色位置、旋转角度、大小
8. 导出当前场景为PNG图片保存到本地

---

## 3. 功能规格

### 3.1 角色生成模块

#### 3.1.1 图片上传
- **上传方式**：点击或拖拽到上传区域
- **格式限制**：JPG、PNG
- **大小限制**：≤ 5MB
- **验证反馈**：
  - 格式不符 → 错误弹窗："不支持的文件格式，请上传JPG或PNG格式的图片"
  - 大小超限 → 错误弹窗："图片大小超过5MB限制，请压缩后重新上传"
  - 上传成功 → 显示原图预览 + 文件名

#### 3.1.2 角色信息表单
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 角色名称 | 文本输入 | 是 | 如"钟离"、"Iron Man" |
| 角色出处 | 文本输入 | 否 | 如"《原神》"、"《复仇者联盟》" |
| 角色描述 | 多行文本 | 否 | 简单描述角色特点 |
| 经典台词 | 多行文本 | 否 | 角色的标志性台词 |

#### 3.1.3 像素化生成算法
MVP阶段使用**前端Canvas纯算法**，不依赖AI接口：

```
算法流程：
1. 将上传图片缩放到固定尺寸（如64x64或128x128）
2. 将图片绘制到离屏Canvas
3. 按像素块大小（如4x4或8x8）对图像进行分块
4. 每个块取平均颜色，替换整个块为该纯色
5. 可选：应用调色板限制（限制颜色数量，增强像素感）
6. 可选：添加轮廓线增强Q版感
7. 输出像素化后的图像
```

**参数配置**：
- 像素块大小：默认8x8（用户可在设置中调整4x4 / 8x8 / 16x16）
- 调色板颜色数：默认32色
- 生成时间目标：≤ 2秒

**生成后操作**：
- 确认保存 → 存入IndexedDB角色库
- 重新生成 → 用不同参数再次像素化
- 导出单角色 → 下载单个角色的PNG图片

### 3.2 场景编辑模块

#### 3.2.1 布局结构（三栏）
```
┌──────────┬────────────────────────┬──────────┐
│  资源面板  │      Canvas画布        │  属性面板  │
│ (左侧2/12)│     (中间8/12)         │ (右侧2/12)│
│          │                        │          │
│ 📦 角色库 │   [网格背景/场景模板]     │ 名称      │
│ [角色1]  │                        │ X坐标     │
│ [角色2]  │    🧍‍♂️  🧍‍♀️            │ Y坐标     │
│ [角色3]  │       🧍               │ 旋转角度   │
│          │                        │ 缩放大小   │
│ 🏞️ 场景  │                        │          │
│ [客厅]   │                        │ [删除]    │
│ [草地]   │                        │          │
│ [海滩]   │                        │          │
└──────────┴────────────────────────┴──────────┘
```

#### 3.2.2 Canvas画布
- **画布尺寸**：800×600px（可滚动扩展）
- **网格背景**：20×20px网格线辅助对齐
- **场景模板背景**：预设像素风场景图覆盖在网格之上
- **操作**：
  - 点击选中角色
  - 鼠标拖拽移动角色位置
  - 选中后可用滑块/输入框调整旋转（0°-360°）和缩放（50%-200%）
  - 支持多选（Ctrl+点击）

#### 3.2.3 预设场景模板（MVP提供4个）
| 场景名称 | 描述 | 色调 |
|---------|------|------|
| 温馨客厅 | 像素风的温馨客厅，有沙发和窗户 | 暖色调 |
| 绿色草地 | 户外草地，蓝天白云 | 绿色/蓝色 |
| 阳光海滩 | 沙滩和海浪 | 黄色/蓝色 |
| 纯色网格 | 干净的网格背景（默认） | 灰白色 |

所有场景模板使用像素风格绘制，尺寸与Canvas画布匹配（800×600px）。

#### 3.2.4 拖拽交互细节
- 拖拽角色时显示半透明虚影
- 接近画布边缘时自动扩展画布
- 拖拽到画布外区域时角色回到原位
- 支持撤销（Ctrl+Z，最多10步）

#### 3.2.5 属性面板
选中角色后显示并可编辑：
- 角色名称（只读，显示角色库中的名称）
- X/Y坐标（数值输入）
- 旋转角度（0°-360°，滑块+输入框）
- 缩放大小（50%-200%，滑块+输入框）
- 层级顺序（置顶/置底按钮）
- 删除按钮（红色，二次确认）

### 3.3 资源管理模块

#### 3.3.1 角色库视图
- 卡片网格布局（响应式：2/4/6列）
- 每张卡片显示：像素图缩略图、角色名称、出处
- 操作按钮：编辑信息、导出单图、删除（二次确认）
- 搜索/筛选：按名称搜索、按出处筛选
- 排序：按创建时间/名称排序

#### 3.3.2 场景库视图（MVP简化版）
- 当前场景缩略图预览
- 场景名称和创建/修改时间
- MVP阶段仅支持单一当前场景（多场景留待后续版本）

### 3.4 场景导出模块

- **导出格式**：PNG
- **导出内容**：当前Canvas画布的全部内容（背景模板 + 所有角色）
- **导出尺寸**：与画布实际尺寸一致（800×600px，2x分辨率可选1600×1200）
- **导出流程**：
  1. 点击"导出场景图片"按钮
  2. 弹出分辨率选择（1x / 2x）
  3. 使用Canvas.toDataURL()生成图片
  4. 触发浏览器下载
- **成功反馈**：成功弹窗提示"场景已成功导出为PNG图片"

### 3.5 错误处理

| 场景 | 处理方式 |
|------|---------|
| 图片格式错误 | 错误弹窗 + 说明原因 |
| 图片大小超限 | 错误弹窗 + 说明限制 |
| 未输入角色名称 | 内联提示"请输入角色名称" |
| IndexedDB存储空间不足 | 提示用户清理旧数据 |
| Canvas渲染失败 | 错误弹窗 + 提供重试按钮 |
| 浏览器不支持IndexedDB | 降级使用LocalStorage |

---

## 4. 数据模型

### 4.1 角色数据 (characters)
```javascript
{
  id: string,           // 唯一标识（UUID）
  name: string,         // 角色名称（必填）
  source: string,       // 角色出处
  description: string,  // 角色描述
  quote: string,        // 经典台词
  originalImage: string, // 原始上传图片（Base64 DataURL）
  pixelImage: string,   // 像素化后的图片（Base64 DataURL）
  pixelSize: number,    // 像素块大小（如8）
  createdAt: string,    // 创建时间（ISO格式）
  updatedAt: string     // 更新时间
}
```

### 4.2 场景数据 (scenes)
```javascript
{
  id: string,           // 唯一标识
  name: string,         // 场景名称
  template: string,     // 使用的场景模板ID
  width: number,        // 画布宽度
  height: number,       // 画布高度
  placements: [         // 角色摆放列表
    {
      characterId: string,  // 关联角色ID
      x: number,            // X坐标
      y: number,            // Y坐标
      rotation: number,     // 旋转角度（0-360）
      scale: number,        // 缩放比例（0.5-2.0）
      zIndex: number        // 层级顺序
    }
  ],
  createdAt: string,
  updatedAt: string
}
```

### 4.3 存储方案
- **主存储**：IndexedDB
  - 数据库名：`character-land-db`
  - 对象存储：`characters`、`scenes`、`settings`
- **降级方案**：LocalStorage（当IndexedDB不可用时）
- **存储限制监控**：在应用启动时检查可用空间，不足时提醒用户

---

## 5. 界面设计

### 5.1 设计系统
- **配色方案**：黑白灰为主色调（Material Design灰阶）
  - 主色：#212121（深黑）
  - 次要色：#757575（中灰）
  - 边框：#E0E0E0（浅灰）
  - 背景：#F5F5F5（浅灰背景）
  - 错误色：#D32F2F
  - 成功色：#388E3C
- **字体**：Inter / 系统默认无衬线字体
- **风格**：简洁、克制，突出角色内容本身

### 5.2 页面结构
```
┌──────────────────────────────────────┐
│  顶部导航栏（Logo + 三个Tab切换）        │
├──────────────────────────────────────┤
│                                      │
│        主内容区（根据Tab切换）            │
│                                      │
│  角色生成 | 场景编辑 | 资源管理          │
│                                      │
├──────────────────────────────────────┤
│  状态栏/提示信息（可选）                 │
└──────────────────────────────────────┘
```

### 5.3 响应式策略
- **桌面端（≥1024px）**：完整三栏布局，优先体验
- **平板端（768-1023px）**：Canvas区域占主要空间，侧面板可折叠
- **手机端（<768px）**：以基础浏览为主，提示使用桌面端获得完整体验

---

## 6. 技术架构

### 6.1 技术选型
| 层面 | 技术选择 | 理由 |
|------|---------|------|
| 框架 | 纯HTML/CSS/JS（或React） | MVP优先轻量，原型已用原生JS |
| CSS | Tailwind CSS（CDN） | 快速开发，与原型一致 |
| Canvas | 原生Canvas API | 像素化算法 + 场景渲染 |
| 存储 | IndexedDB（Dexie.js封装） | 大容量本地存储 |
| 拖拽 | 原生Drag & Drop API | 无额外依赖 |
| 构建 | 无需构建（单文件/多文件HTML） | MVP简单部署 |

### 6.2 MVP技术决策
- **不使用框架**：基于原型的原生JS + Tailwind CSS继续开发，降低复杂度
- **不使用后端**：纯前端应用，所有数据本地存储
- **不使用AI接口**：Canvas算法实现像素化，零API成本
- **部署方式**：静态HTML文件，可直接用浏览器打开或部署到GitHub Pages/Vercel

### 6.3 文件结构
```
character-world/
├── index.html              # 主页面（应用入口）
├── css/
│   └── styles.css          # 自定义样式（补充Tailwind）
├── js/
│   ├── app.js              # 应用初始化 & Tab切换
│   ├── upload.js            # 图片上传处理
│   ├── pixelate.js          # 像素化算法
│   ├── canvas-editor.js     # 场景编辑器（Canvas操作）
│   ├── drag-drop.js         # 拖拽功能
│   ├── storage.js           # IndexedDB数据存取
│   ├── export.js            # 场景导出
│   └── ui.js               # UI辅助（弹窗、提示等）
├── assets/
│   ├── scenes/              # 预设场景模板图片
│   │   ├── living-room.png
│   │   ├── grassland.png
│   │   ├── beach.png
│   │   └── grid.png
│   └── icons/               # 图标资源
└── design-document.md       # 本设计文档
```

---

## 7. 像素化算法设计

### 7.1 核心流程
```
原图 → 缩放（缩略图尺寸）→ 分块 → 取平均色 → 色板限制 → 轮廓增强 → 像素图输出
```

### 7.2 详细步骤
1. **图片加载**：将上传的图片加载到Image对象
2. **等比缩放**：缩放到目标尺寸（如128×128），保持宽高比，居中裁剪
3. **分块采样**：按pixelSize（如8px）将图片分为若干块
4. **颜色平均**：每个块内所有像素的RGB取平均值
5. **色板映射**（可选）：将平均色映射到最近的调色板颜色
6. **轮廓增强**（可选）：检测色块边缘，添加深色轮廓线
7. **渲染输出**：绘制到输出Canvas上

### 7.3 可调参数
| 参数 | 默认值 | 范围 | 效果 |
|------|--------|------|------|
| 输出尺寸 | 128px | 64-256 | 输出像素图的分辨率 |
| 像素块大小 | 8px | 4-16 | 越大像素感越强 |
| 颜色数量 | 32 | 8-64 | 越少越像素复古 |
| 轮廓线 | 开启 | 开/关 | 增强角色轮廓 |

---

## 8. 场景模板规范

### 8.1 模板图片要求
- 格式：PNG（支持透明通道可选）
- 尺寸：800×600px（与默认画布匹配）
- 风格：像素风
- 色调：保持整体和谐，不抢角色风头

### 8.2 MVP提供的4个模板
1. **温馨客厅**：像素风室内场景，暖色调，包含沙发、窗户、地毯
2. **绿色草地**：户外自然场景，包含草地、天空、云朵
3. **阳光海滩**：海边场景，包含沙滩、海水、阳光
4. **纯色网格**：透明背景 + 20px网格线，最简洁的展示方式

### 8.3 场景模板扩展（后续版本）
- 更多场景：咖啡馆、图书馆、城堡、太空等
- 用户自定义场景上传
- 场景组件系统（地面、墙壁、家具、装饰品）

---

## 9. 开发计划

### 9.1 开发阶段

| 阶段 | 内容 | 预计产出 |
|------|------|---------|
| **Phase 1：基础框架** | 页面结构搭建、Tab切换、Tailwind集成 | 可导航的静态页面 |
| **Phase 2：角色上传** | 图片上传、表单验证、原图预览 | 完成上传+信息填写流程 |
| **Phase 3：像素化算法** | Canvas像素化核心算法、预览、参数调整 | 可生成像素图 |
| **Phase 4：数据存储** | IndexedDB集成、角色CRUD、数据持久化 | 角色数据本地保存 |
| **Phase 5：场景编辑器** | Canvas画布、拖拽/旋转/缩放、场景模板 | 可编辑角色场景 |
| **Phase 6：导出功能** | Canvas导出PNG、分辨率选择、下载触发 | 可导出场景图片 |
| **Phase 7：错误处理** | 各种异常场景的提示和处理 | 健壮的用户体验 |
| **Phase 8：测试优化** | 跨浏览器测试、性能优化、细节打磨 | 可发布的MVP |

### 9.2 技术依赖
- **必须**：现代浏览器（Chrome / Edge / Firefox 最新版），支持Canvas和IndexedDB
- **可选**：CDN加载的Tailwind CSS、Dexie.js（IndexedDB封装库）
- **无需**：服务器、数据库、API密钥、构建工具

---

## 10. 后续版本规划（超出MVP范围）

- **AI增强生成**：接入DeepSeek等AI模型，实现真正的Q版重绘
- **用户账号系统**：注册登录，云端同步数据
- **多场景管理**：创建和管理多个场景空间
- **社交分享**：生成分享链接，社区展示
- **场景组件市场**：用户可创建和分享场景组件
- **角色动画**：像素小人添加简单动画（待机、走路）
- **移动端适配**：完整的手机端体验

---

## 11. 验证方案

### 11.1 功能验证
1. **角色生成流程**：上传图片 → 填写信息 → 生成像素图 → 保存 → 在角色库中可见
2. **场景编辑流程**：从角色库拖拽角色到画布 → 移动/旋转/缩放 → 切换场景模板 → 角色位置正确保留
3. **导出流程**：点击导出 → 选择分辨率 → 浏览器下载PNG → 图片内容与画布一致
4. **数据持久化**：关闭浏览器 → 重新打开 → 角色库和场景数据仍在
5. **错误处理**：上传非法文件 → 看到明确错误提示 → 可重试

### 11.2 性能验证
- 角色生成耗时 ≤ 2秒（128px输出尺寸）
- Canvas拖拽/旋转响应延迟 < 100ms
- 页面加载时间 < 3秒

### 11.3 兼容性验证
- Chrome（最新版）：功能完整
- Edge（最新版）：功能完整
- Firefox（最新版）：功能完整
- 手机浏览器：页面可访问，基本内容可见
